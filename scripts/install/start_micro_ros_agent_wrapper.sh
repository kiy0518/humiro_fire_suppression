#!/bin/bash
# Micro-ROS Agent wrapper script
# 이 파일은 더 이상 사용되지 않습니다.
# 실제 wrapper는 003-apply_config.sh에 의해 runtime 폴더에 생성됩니다.
# 
# 사용: sudo ./003-apply_config.sh를 실행하여 최신 wrapper 생성

echo "ERROR: 이 스크립트는 더 이상 사용되지 않습니다."
echo "runtime 폴더의 wrapper를 사용하거나, 003-apply_config.sh를 실행하세요."
echo ""
echo "실행 방법:"
echo "  cd /home/khadas/humiro_fire_suppression/scripts/install"
echo "  sudo ./003-apply_config.sh"
exit 1
